TributeBot is a first attempt at a twitter bot using Tweepy
and the streaming API.

It looks for tweets that match /(\w+) for the \1 god/ (along
with a few variants) and then retweets them as @tributebot.
